# SIH-DoJ-Chatbot-by-CoJ
```
SIH-DoJ-Chatbot-by-CoJ/
├── actions/                  # Custom actions folder
│   └── __init__.py
├── config.yml                # Rasa configuration file
├── credentials.yml           # Credentials for external services
├── data/                     # NLU training data
│   └── nlu.yml
├── domain.yml                # Domain file defining intents, entities, slots, etc.
├── endpoints.yml             # Configuration for external services and endpoints
├── models/                   # Trained models
├── rasa_chatbot/             # Virtual environment (To be set up with requirements.txt)
├── tests/                    # Unit tests
│   └── test_stories.yml
└── requirements.txt          # Python dependencies
```
